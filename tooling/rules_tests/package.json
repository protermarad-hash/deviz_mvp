{
  "name": "deviz-mvp-rules-tests",
  "private": true,
  "description": "FAZA 1.1 - teste izolate pentru firestore.rules/storage.rules (Firebase Emulator Suite, proiect demo- local, fara acces la productie).",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --project demo-invoice-rules-test --config firebase.rules-test.json \"node --test --test-concurrency=1 tooling/rules_tests/firestore.rules.test.js tooling/rules_tests/storage.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0"
  }
}
