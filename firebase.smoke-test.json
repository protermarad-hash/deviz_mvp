{
  "$comment": "Config EXCLUSIV pentru smoke-test-ul FAZA 3 (Import materiale din factura), local, Firebase Emulator Suite. NU e firebase.json-ul proiectului (acela ramane complet neatins). NU se foloseste NICIODATA cu `firebase deploy`. Trebuie sa stea la radacina repo-ului (Firebase CLI nu permite path-uri de rules care escapeaza directorul configului). Foloseste EXACT firestore.rules/storage.rules reale si functions/ reale (parseSupplierInvoiceXml).",
  "firestore": {
    "rules": "firestore.rules"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "functions": {
    "source": "functions"
  },
  "emulators": {
    "auth": {
      "port": 5307,
      "host": "127.0.0.1"
    },
    "firestore": {
      "port": 5308,
      "host": "127.0.0.1"
    },
    "storage": {
      "port": 5309,
      "host": "127.0.0.1"
    },
    "functions": {
      "port": 5310,
      "host": "127.0.0.1"
    },
    "ui": {
      "enabled": false
    },
    "singleProjectMode": true
  }
}
